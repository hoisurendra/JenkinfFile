pipeline {
    agent any
    stages {
        stage('Welcome Step') {
            steps { 
                echo 'Testing the GitHub Repo & Jenkins Integration'
            }
        }
    }
}
